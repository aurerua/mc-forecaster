# Jira credentials (Jira Cloud only)
# Generate your API token at: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_URL=https://yourcompany.atlassian.net
JIRA_EMAIL=you@yourcompany.com
JIRA_TOKEN=your-api-token

# mcf defaults
MCF_DEFAULT_PROJECT=MYPROJECT
# Every issue type your org uses (the full "universe"). Required for Jira runs;
# --exclude-types / --exclude-epic-types subtract from this. If unset, mcf errors
# and lists your project's issue types so you can paste them here.
MCF_JIRA_TYPES=Bug,Story,UX Story,UX Debt,Technical Debt,Task,Spike,Security Vulnerability

# Workday vacation integration (optional — required only when using --workday)
# WD_JSON_LINK is the base URL of your Workday RaaS report, including any org-specific
# filter parameters. Do NOT include the date range parameters or &format=json — mcf
# appends those automatically using WD_PARAM_DATE_FROM and WD_PARAM_DATE_TO below.
WD_JSON_LINK=https://services1.wd502.myworkday.com/ccx/service/customreport2/yourcompany/ReportName/VacationReport?OtherFilter=value
WD_USER=your-workday-integration-user
WD_PASSWORD=your-workday-password

# Comma-separated list of worker names to exclude from vacation counting (e.g. non-engineers)
# WD_EXCLUDE_WORKERS=Alice Smith,Bob Jones

# Field names in the JSON response — must match the column labels in your Workday report.
WD_FIELD_ENTRIES=Report_Entry
WD_FIELD_WORKER=Worker
WD_FIELD_DATE_FROM=From
WD_FIELD_DATE_TO=To

# URL query parameter names used to filter the date range in WD_JSON_LINK.
WD_PARAM_DATE_FROM=Event_Effective_Date_On_or_After
WD_PARAM_DATE_TO=Event_Effective_Date_On_or_Before
